import { UnicodeCleanPipe } from './unicode-clean.pipe';

describe('UnicodeCleanPipe', () => {
  it('create an instance', () => {
    const pipe = new UnicodeCleanPipe();
    expect(pipe).toBeTruthy();
  });
});
